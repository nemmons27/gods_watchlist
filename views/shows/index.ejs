<%- include('../partials/header') %>



    <p>Shows</p>
    <ul>
        <% shows.forEach((show) => { %>
        <li>
            <a href="/shows/<%= show.name %>"><%= show.category %></a>
            - 
            <% show.done ? 'done' : 'not done' %>
        </li>
        <% }) %>
    </ul>
    <a href="/shows/new">Add Show</a>
    
<%- include('../partials/footer')%>

