<%- include('.partials/header') %>

    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <ul>
        <% shows.forEach(function(t) { %>
        <li>
            <%= s.category %>
        </li>
        <% }); %>
    </ul>

<%- include('.partials/footer') %>